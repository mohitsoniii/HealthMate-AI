# ─────────────────────────────────────────────
# HealthMate AI — Requirements
# Python 3.10+ required
# Last verified: May 2025
# ─────────────────────────────────────────────

# ── Core UI ───────────────────────────────────
streamlit>=1.45.0

# ── LangChain (latest modular stack) ─────────
# NOTE: langchain 0.x is deprecated.
# The modern stack uses separate provider packages.
langchain>=1.2.18
langchain-core>=1.4.0
langchain-community>=0.4.1
langchain-ollama>=1.1.0          # Replaces deprecated langchain_community.llms.Ollama
langchain-groq>=0.3.2

# ── Vector Store ─────────────────────────────
faiss-cpu>=1.13.2

# ── Embeddings ───────────────────────────────
torch
torchvision
sentence-transformers>=3.4.1

# ── PDF Extraction (cascading fallback) ──────
pymupdf>=1.25.5                  # PyMuPDF — fastest, best quality
pdfplumber>=0.11.5               # Good for tables
pypdf>=5.4.0                     # Lightweight fallback

# ── Core Utilities ────────────────────────────
numpy>=2.0.0
requests>=2.32.0
